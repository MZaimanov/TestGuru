<tr>
  <td><%= question.body %></td>
  <td><%= link_to 'Просмотр', question_path(question) %></td>
  <td><%= link_to 'Редактирование', edit_question_path(question) %></td>
  <td><%= link_to 'Удалить', question_path(question), method: :delete,
        data: { confirm: 'Вы уверены?' } %>
</tr>
