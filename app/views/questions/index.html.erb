<h1>Test Questions</h1>

<p>
  <%= link_to 'Новй вопрос', new_test_question_path %>
</p>

<table>
  <% @test.questions.each do |question| %>
    <tr>
      <td><%= question.body %></td>
      <td><%= link_to 'Прсмотр', test_question_path(@test, question) %></td>
      <td><%= link_to 'Редактирование', edit_test_question_path(@test, question) %></td>
      <td><%= link_to 'Удалить', test_question_path(@test, question), method: :delete,
        data: { confirm: 'Вы уверены?' } %>
    </tr>
  <% end %>
</table>

<%= link_to 'Назад', test_path(@test) %>
